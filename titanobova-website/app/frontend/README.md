Titanobova Frontend (Vite + React + Tailwind)

1. Install dependencies:

   npm install

2. Run dev server:

   npm run dev

3. The frontend expects the backend at `http://localhost:4000` by default. Adjust requests in code if you change the backend port.

4. Host login: use credentials you set in backend `.env` (`HOST_USERNAME` / `HOST_PASSWORD`).
